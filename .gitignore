# Dependencies
node_modules

# Build output (Docker rebuilds this from src)
dist

# Local secrets — .env.example and .env.test are committed
.env

# macOS
.DS_Store

# VSCode settings
.vscode

# Context docs are tracked; the source PDF is not
context/*.pdf
